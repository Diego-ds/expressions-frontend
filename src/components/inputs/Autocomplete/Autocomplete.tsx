import { Option } from "../../../models/Input.model";

interface ExtraAutocompleteProps {
  options?: Option[];
  isLoadingOptions?: boolean;
}

const Autocomplete = () => {
  
}

export default Autocomplete;